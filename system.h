#pragma once
#include <stdio.h>
#include <string.h>




void summa(char* firstnumber, char* secondnumber, char* result);
void vichitanie(char* firstnumber, char* secondnumber, char* result);
int proverka(char* firstnumber, char* secondnumber);
int zero(char* number);
void ltoae(long num, char* s, int res, int* x);
